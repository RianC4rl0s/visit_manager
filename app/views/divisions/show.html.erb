<%= render partial: "shared/appBar" %>
<div class="page-container">
  <p style="color: green"><%= notice %></p>
  <%= render @division %>
  <div>
    <%= link_to "Editar", edit_division_path(@division) %> |
    <%= link_to "Divisões", divisions_path %>
    <%= button_to "Deletar", @division, method: :delete, class:"btn btn-danger" %>
  </div>
</div>
